{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "PY=\"$ANVIL_PYTHON\"; [ -n \"$PY\" ] || { P3=$(command -v python3 2>/dev/null); if [ -n \"$P3\" ] && [ -s \"$P3\" ]; then PY=python3; else PY=python; fi; }; \"$PY\" \"${CLAUDE_PLUGIN_ROOT}/scripts/anvil-gate-commit.py\""
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "PY=\"$ANVIL_PYTHON\"; [ -n \"$PY\" ] || { P3=$(command -v python3 2>/dev/null); if [ -n \"$P3\" ] && [ -s \"$P3\" ]; then PY=python3; else PY=python; fi; }; \"$PY\" \"${CLAUDE_PLUGIN_ROOT}/scripts/anvil-track-edit.py\""
          }
        ]
      }
    ]
  }
}
